# Exercise 6
Create a class `Product` with attributes `name`, `price`, `quantity`. Add a method to calculate the total cost of all quantities.

# Exercise 7
Create a class `Customer` with attributes `name`, `age`, `city`. Add a method that checks if the customer is eligible for a loyalty program (`age > 25`).

# Exercise 8
Create a `BankAccount` class that supports:
- deposit
- withdraw
- check balance
- display transaction logs

# Exercise 9
Create a class `Mobile` with attributes `brand`, `model`, `storage`. Add a method to upgrade storage.

# Exercise 10
Create a class `Student` with three subject marks. Add methods to calculate:
- total
- percentage
- grade (`A`, `B`, `C`, `D`)
